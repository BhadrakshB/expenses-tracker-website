---
import BaseLayout from "../layouts/BaseLayout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import PricingCard from "../components/PricingCard.astro";
import FAQAccordion from "../components/FAQAccordion.astro";
import SectionHeading from "../components/SectionHeading.astro";
import BrutalButton from "../components/BrutalButton.astro";

// Server-side A/B variant
let variant = "A";
const cookieVariant = Astro.cookies.get("hero_variant")?.value;
if (cookieVariant && (cookieVariant === "A" || cookieVariant === "B")) {
  variant = cookieVariant;
}

const freeFeatures = [
  { text: "Unlimited expenses", included: true },
  { text: "Categories & tags", included: true },
  { text: "1 bank account", included: true },
  { text: "Offline storage", included: true },
  { text: "CSV export (1×/week)", included: true },
  { text: "Biometric lock", included: true },
  { text: "Cloud sync", included: false },
  { text: "AI insights", included: false },
  { text: "SMS tracking", included: false },
  { text: "Bill reminders", included: false },
];

const proFeatures = [
  { text: "Everything in Free", included: true },
  { text: "Unlimited bank accounts", included: true },
  { text: "Cloud sync across devices", included: true },
  { text: "AI-powered insights", included: true },
  { text: "Automatic SMS tracking", included: true },
  { text: "Bill & EMI reminders", included: true },
  { text: "Family sharing (up to 5)", included: true },
  { text: "Unlimited exports", included: true },
  { text: "Priority support", included: true },
  { text: "Early access to features", included: true },
];

const faqs = [
  {
    question: "What happens when my trial ends?",
    answer:
      "You'll automatically switch to the Free plan. <strong>No charges</strong>. All your data remains safe, but Pro features become unavailable until you upgrade.",
  },
  {
    question: "Can I cancel anytime?",
    answer:
      "Absolutely! Cancel anytime from your device's subscription settings. You'll retain Pro access until the end of your billing period.",
  },
  {
    question: "How do refunds work?",
    answer:
      'We offer a <strong>14-day full refund</strong> policy. If you\'re not satisfied, contact us or request a refund through the App Store/Play Store. See our <a href="/refunds">refund policy</a> for details.',
  },
  {
    question: "Is my financial data secure?",
    answer:
      "Your security is our priority. We use <strong>TLS 1.3</strong> encryption in transit and <strong>AES-256</strong> encryption at rest. We never sell your data and follow GDPR/CCPA guidelines.",
  },
  {
    question: "Does the app work offline?",
    answer:
      "Yes! Both Free and Pro work fully offline. Pro users get automatic sync when connected. Your data is always available, internet or not.",
  },
  {
    question: "How does family sharing work?",
    answer:
      "Pro subscribers can invite up to 5 family members. Each gets their own account with full Pro features. Share budgets and track household expenses together.",
  },
];

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  mainEntity: faqs.map((faq) => ({
    "@type": "Question",
    name: faq.question,
    acceptedAnswer: {
      "@type": "Answer",
      text: faq.answer.replace(/<[^>]*>/g, ""),
    },
  })),
};
---

<BaseLayout
  title="Simple, Honest Pricing"
  description="Choose the plan that fits your needs. Start free forever or unlock everything with Pro. 14-day free trial, cancel anytime."
  canonical="/pricing"
>
  <Fragment slot="structured-data">
    <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
  </Fragment>

  <Header slot="header" />

  {variant === "A" ? (
    <!-- VARIANT A: Original Brutal Design -->
    <>
      <!-- Pricing Hero -->
      <section class="pricing-hero">
        <div class="container">
          <h1 class="pricing-title">
            SIMPLE, HONEST<br /><span class="title-accent">PRICING</span>
          </h1>
          <p class="pricing-subtitle">
            No hidden fees. No surprises. Cancel anytime.
          </p>
        </div>
      </section>

      <!-- Pricing Cards -->
      <section class="pricing-cards-section">
        <div class="container">
          <div class="pricing-grid">
            <div class="card-wrapper card-pro">
              <div class="recommended-ribbon">RECOMMENDED</div>
              <PricingCard
                name="Pro"
                price="$4.99"
                period="month"
                description="Unlock the full power of expense tracking"
                features={proFeatures}
                ctaText="Start 14-Day Free Trial"
                ctaHref="https://apps.apple.com/app/expenses-tracker"
                highlighted
                badge="Most Popular"
              />
            </div>
            <div class="card-wrapper card-free">
              <PricingCard
                name="Free"
                price="$0"
                description="Everything you need to get started"
                features={freeFeatures}
                ctaText="Download Free"
                ctaHref="https://apps.apple.com/app/expenses-tracker"
              />
            </div>
          </div>

          <div class="yearly-offer">
            <div class="offer-badge">SAVE 20%</div>
            <p class="offer-text">
              Pay yearly for <strong>$49.99/year</strong>
              <span class="offer-savings">(That's $4.17/month)</span>
            </p>
          </div>
        </div>
      </section>

      <!-- FAQ Section -->
      <section class="faq-section section-dark">
        <div class="container container-narrow">
          <SectionHeading
            title="Questions?"
            subtitle="Everything you need to know about Expenses Tracker Pro."
            align="center"
          />
          <div class="faq-list">
            {faqs.map((faq) => (
              <FAQAccordion question={faq.question} answer={faq.answer} />
            ))}
          </div>
        </div>
      </section>

      <!-- Guarantee Banner -->
      <section class="guarantee-section">
        <div class="container">
          <div class="guarantee-banner">
            <div class="guarantee-icon">🛡️</div>
            <div class="guarantee-content">
              <h3 class="guarantee-title">30-Day Money-Back Guarantee</h3>
              <p class="guarantee-text">
                Not happy? Get a full refund within 30 days. No questions asked.
                We're confident you'll love Pro, but if not, we've got you covered.
              </p>
            </div>
            <BrutalButton href="/refunds" variant="secondary" size="sm">
              Learn More
            </BrutalButton>
          </div>
        </div>
      </section>
    </>
  ) : (
    <!-- VARIANT B: Apple-Style Light Theme -->
    <>
      <!-- Pricing Hero B -->
      <section class="pricing-hero-b">
        <div class="container">
          <span class="page-badge-b">💰 Pricing</span>
          <h1 class="pricing-title-b">
            Simple, honest pricing.
          </h1>
          <p class="pricing-subtitle-b">
            No hidden fees. No surprises. Cancel anytime.
          </p>
        </div>
      </section>

      <!-- Pricing Cards B -->
      <section class="pricing-cards-section-b">
        <div class="container">
          <div class="pricing-grid-b">
            <!-- Most Opted Indicator (mobile only) -->
            <div class="most-opted-indicator">
              <span class="opted-icon">🏆</span>
              <span class="opted-text">Most users choose Pro</span>
            </div>

            <!-- Pro Card (First on mobile) -->
            <div class="pricing-card-b pro">
              <div class="popular-badge-b">Most Popular</div>
              <div class="card-header-b">
                <h3 class="plan-name-b">Pro</h3>
                <div class="plan-price-b">
                  <span class="price-amount-b">$4.99</span>
                  <span class="price-period-b">/month</span>
                </div>
                <p class="plan-description-b">Unlock the full power of expense tracking</p>
              </div>
              <ul class="features-list-b">
                {proFeatures.map((feature) => (
                  <li class="included">
                    <span class="feature-icon">✓</span>
                    {feature.text}
                  </li>
                ))}
              </ul>
              <a href="https://apps.apple.com/app/expenses-tracker" class="cta-btn-b primary">
                Start 14-Day Free Trial
              </a>
            </div>

            <!-- Free Card (Second on mobile) -->
            <div class="pricing-card-b free-card">
              <div class="card-header-b">
                <h3 class="plan-name-b">Free</h3>
                <div class="plan-price-b">
                  <span class="price-amount-b">$0</span>
                  <span class="price-period-b">/forever</span>
                </div>
                <p class="plan-description-b">Everything you need to get started</p>
              </div>
              <ul class="features-list-b">
                {freeFeatures.map((feature) => (
                  <li class={feature.included ? "included" : "excluded"}>
                    <span class="feature-icon">{feature.included ? "✓" : "—"}</span>
                    {feature.text}
                  </li>
                ))}
              </ul>
              <a href="https://apps.apple.com/app/expenses-tracker" class="cta-btn-b secondary">
                Download Free
              </a>
            </div>
          </div>

          <div class="yearly-offer-b">
            <span class="offer-badge-b">Save 20%</span>
            <p class="offer-text-b">
              Pay yearly for <strong>$49.99/year</strong> — that's just $4.17/month
            </p>
          </div>
        </div>
      </section>

      <!-- FAQ Section B -->
      <section class="faq-section-b">
        <div class="container container-narrow">
          <div class="section-header-b">
            <h2 class="section-title-b">Questions?</h2>
            <p class="section-subtitle-b">Everything you need to know about Pro.</p>
          </div>
          <div class="faq-list-b">
            {faqs.map((faq, index) => (
              <details class="faq-item-b">
                <summary class="faq-question-b">{faq.question}</summary>
                <div class="faq-answer-b" set:html={faq.answer} />
              </details>
            ))}
          </div>
        </div>
      </section>

      <!-- Guarantee Banner B -->
      <section class="guarantee-section-b">
        <div class="container">
          <div class="guarantee-card-b">
            <div class="guarantee-icon-b">🛡️</div>
            <h3 class="guarantee-title-b">30-Day Money-Back Guarantee</h3>
            <p class="guarantee-text-b">
              Not happy? Get a full refund within 30 days. No questions asked.
            </p>
            <a href="/refunds" class="guarantee-link-b">Learn about our refund policy →</a>
          </div>
        </div>
      </section>
    </>
  )}

  <Footer slot="footer" />
</BaseLayout>

<style>
  /* ========================================
     VARIANT A STYLES (Original Brutal Design)
     ======================================== */
  
  /* Pricing Hero */
  .pricing-hero {
    padding-block: clamp(4rem, 10vh, 6rem) clamp(2rem, 5vh, 4rem);
    text-align: center;
  }

  .pricing-title {
    font-size: clamp(2.5rem, 8vw, 5rem);
    font-weight: 900;
    line-height: 1;
    letter-spacing: -0.03em;
    margin-bottom: 1rem;
  }

  .title-accent {
    color: var(--accent-primary);
  }

  .pricing-subtitle {
    font-size: 1.25rem;
    color: var(--text-secondary);
  }

  /* Pricing Cards */
  .pricing-cards-section {
    padding-block: 2rem 4rem;
    overflow: hidden;
  }

  .pricing-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    max-width: 900px;
    margin: 0 auto;
    align-items: start;
  }

  @media (min-width: 768px) {
    .pricing-grid {
      grid-template-columns: repeat(2, 1fr);
      align-items: start;
    }
  }

  /* Card Wrapper - Desktop Highlighting */
  .card-wrapper {
    position: relative;
  }

  .card-pro {
    order: 1;
    padding-top: 1rem; /* Space for "Most Popular" badge */
  }

  .card-free {
    order: 2;
  }

  /* Desktop: Pro card highlighting effects */
  @media (min-width: 768px) {
    .card-free {
      order: 1;
    }

    .card-pro {
      order: 2;
      transform: scale(1.05) translateY(-12px);
      z-index: 10;
    }

    /* Spotlight effect */
    .card-pro::before {
      content: "";
      position: absolute;
      inset: -20px;
      background: radial-gradient(ellipse at center, var(--accent-primary) 0%, transparent 70%);
      opacity: 0.15;
      z-index: -1;
      border-radius: 20px;
    }
  }

  /* Recommended Ribbon */
  .recommended-ribbon {
    display: none;
  }

  @media (min-width: 768px) {
    .recommended-ribbon {
      display: block;
      position: absolute;
      top: 20px;
      right: -35px;
      background: var(--accent-primary);
      color: var(--background-primary);
      font-family: "Manrope", system-ui, sans-serif;
      font-weight: 800;
      font-size: 0.5rem;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      padding: 0.5rem 2.5rem;
      transform: rotate(45deg);
      z-index: 20;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
    }
  }

  .yearly-offer {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    margin-top: 2rem;
    padding: 1rem 1.5rem;
    background: var(--background-secondary);
    border: 2px dashed var(--accent-primary);
    max-width: 500px;
    margin-inline: auto;
  }

  .offer-badge {
    background: var(--accent-primary);
    color: var(--background-primary);
    font-family: "Manrope", system-ui, sans-serif;
    font-weight: 800;
    font-size: 0.625rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    padding: 0.375rem 0.75rem;
    white-space: nowrap;
  }

  .offer-text {
    font-size: 0.9375rem;
    color: var(--text-secondary);
  }

  .offer-text strong {
    color: var(--text-primary);
  }

  .offer-savings {
    color: var(--text-secondary);
    font-size: 0.8125rem;
    opacity: 0.9;
  }

  /* FAQ Section */
  .faq-section {
    padding-block: clamp(4rem, 10vh, 6rem);
  }

  .faq-list {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  /* Guarantee Section */
  .guarantee-section {
    padding-block: clamp(3rem, 8vh, 5rem);
  }

  .guarantee-banner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
    padding: 2rem;
    background: var(--background-secondary);
    border: 4px solid var(--success);
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
  }

  @media (min-width: 768px) {
    .guarantee-banner {
      flex-direction: row;
      text-align: left;
    }
  }

  .guarantee-icon {
    font-size: 3rem;
    flex-shrink: 0;
  }

  .guarantee-content {
    flex-grow: 1;
  }

  .guarantee-title {
    font-family: "Manrope", system-ui, sans-serif;
    font-size: 1.125rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.02em;
    margin-bottom: 0.5rem;
    color: var(--success);
  }

  .guarantee-text {
    font-size: 0.9375rem;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  /* ========================================
     VARIANT B STYLES (Apple-Style Light Theme)
     ======================================== */

  /* Pricing Hero B */
  .pricing-hero-b {
    padding: 6rem 0 4rem;
    background: #ffffff;
    text-align: center;
  }

  .page-badge-b {
    display: inline-block;
    padding: 0.5rem 1rem;
    background: #f5f5f7;
    border-radius: 100px;
    font-size: 0.875rem;
    font-weight: 600;
    color: #86868b;
    margin-bottom: 1.5rem;
  }

  .pricing-title-b {
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-weight: 700;
    color: #1d1d1f;
    letter-spacing: -0.02em;
    margin-bottom: 1rem;
  }

  .pricing-subtitle-b {
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 1.25rem;
    color: #86868b;
  }

  /* Pricing Cards B */
  .pricing-cards-section-b {
    padding: 2rem 0 6rem;
    background: #ffffff;
    overflow: hidden;
  }

  .pricing-grid-b {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    max-width: 900px;
    margin: 0 auto;
    align-items: start;
  }

  @media (min-width: 768px) {
    .pricing-grid-b {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .pricing-card-b {
    position: relative;
    padding: 2.5rem;
    background: #f5f5f7;
    border-radius: 24px;
    display: flex;
    flex-direction: column;
  }

  .pricing-card-b.pro {
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.08), rgba(118, 75, 162, 0.08));
    border: 2px solid #667eea;
    margin-top: 1rem; /* Space for "Most Popular" badge */
  }

  /* Desktop: Pro card highlighting effects */
  @media (min-width: 768px) {
    .pricing-card-b.pro {
      transform: scale(1.05) translateY(-12px);
      z-index: 10;
    }

    /* Spotlight effect for Variant B */
    .pricing-card-b.pro::before {
      content: "";
      position: absolute;
      inset: -20px;
      background: radial-gradient(ellipse at center, #667eea 0%, transparent 70%);
      opacity: 0.12;
      z-index: -1;
      border-radius: 32px;
    }
  }

  /* Recommended Ribbon for Variant B */
  .pricing-card-b.pro::after {
    display: none;
  }

  @media (min-width: 768px) {
    .pricing-card-b.pro::after {
      content: "RECOMMENDED";
      display: block;
      position: absolute;
      top: 24px;
      right: -30px;
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: white;
      font-family: "Inter", -apple-system, system-ui, sans-serif;
      font-weight: 700;
      font-size: 0.5rem;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      padding: 0.5rem 2rem;
      transform: rotate(45deg);
      z-index: 20;
      box-shadow: 0 2px 8px rgba(102, 126, 234, 0.3);
    }
  }

  .popular-badge-b {
    position: absolute;
    top: -12px;
    left: 50%;
    transform: translateX(-50%);
    padding: 0.5rem 1rem;
    background: linear-gradient(135deg, #667eea, #764ba2);
    color: white;
    border-radius: 100px;
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 0.75rem;
    font-weight: 600;
  }

  .card-header-b {
    text-align: center;
    margin-bottom: 2rem;
    padding-top: 0.5rem;
  }

  .plan-name-b {
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 1.5rem;
    font-weight: 700;
    color: #1d1d1f;
    margin-bottom: 0.5rem;
  }

  .plan-price-b {
    display: flex;
    align-items: baseline;
    justify-content: center;
    gap: 0.25rem;
    margin-bottom: 0.5rem;
  }

  .price-amount-b {
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 3rem;
    font-weight: 700;
    color: #1d1d1f;
  }

  .price-period-b {
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 1rem;
    color: #86868b;
  }

  .plan-description-b {
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 0.9375rem;
    color: #86868b;
  }

  .features-list-b {
    list-style: none;
    padding: 0;
    margin: 0 0 2rem;
    flex-grow: 1;
  }

  .features-list-b li {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.625rem 0;
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 0.9375rem;
    border-bottom: 1px solid rgba(0, 0, 0, 0.05);
  }

  .features-list-b li.included {
    color: #1d1d1f;
  }

  .features-list-b li.excluded {
    color: #86868b;
  }

  .feature-icon {
    width: 20px;
    text-align: center;
    font-weight: 600;
  }

  .features-list-b li.included .feature-icon {
    color: #667eea;
  }

  .cta-btn-b {
    display: block;
    text-align: center;
    padding: 1rem 2rem;
    border-radius: 14px;
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-weight: 600;
    font-size: 1rem;
    transition: all 0.2s ease;
  }

  .cta-btn-b.primary {
    background: linear-gradient(135deg, #667eea, #764ba2);
    color: white;
  }

  .cta-btn-b.primary:hover {
    transform: scale(1.02);
    box-shadow: 0 8px 24px rgba(102, 126, 234, 0.3);
  }

  .cta-btn-b.secondary {
    background: #1d1d1f;
    color: white;
  }

  .cta-btn-b.secondary:hover {
    background: #000000;
    transform: scale(1.02);
  }

  .yearly-offer-b {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    margin-top: 3rem;
    padding: 1rem 2rem;
    background: #f5f5f7;
    border-radius: 16px;
    max-width: 500px;
    margin-inline: auto;
    flex-wrap: wrap;
    text-align: center;
  }

  .offer-badge-b {
    padding: 0.375rem 0.75rem;
    background: linear-gradient(135deg, #667eea, #764ba2);
    color: white;
    border-radius: 100px;
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 0.75rem;
    font-weight: 600;
    white-space: nowrap;
    flex-shrink: 0;
  }

  .offer-text-b {
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 0.9375rem;
    color: #1d1d1f;
    text-align: center;
  }

  .offer-text-b strong {
    color: #667eea;
  }

  /* FAQ Section B */
  .faq-section-b {
    padding: 6rem 0;
    background: #f5f5f7;
  }

  .section-header-b {
    text-align: center;
    margin-bottom: 3rem;
  }

  .section-title-b {
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 2.5rem;
    font-weight: 700;
    color: #1d1d1f;
    margin-bottom: 0.5rem;
  }

  .section-subtitle-b {
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 1.125rem;
    color: #86868b;
  }

  .faq-list-b {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .faq-item-b {
    background: #ffffff;
    border-radius: 16px;
    overflow: hidden;
  }

  .faq-question-b {
    padding: 1.25rem 1.5rem;
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 1rem;
    font-weight: 600;
    color: #1d1d1f;
    cursor: pointer;
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .faq-question-b::after {
    content: "+";
    font-size: 1.5rem;
    color: #86868b;
    transition: transform 0.2s ease;
  }

  .faq-item-b[open] .faq-question-b::after {
    transform: rotate(45deg);
  }

  .faq-answer-b {
    padding: 0 1.5rem 1.25rem;
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 0.9375rem;
    color: #86868b;
    line-height: 1.7;
  }

  .faq-answer-b :global(strong) {
    color: #1d1d1f;
  }

  .faq-answer-b :global(a) {
    color: #667eea;
  }

  /* Guarantee Section B */
  .guarantee-section-b {
    padding: 4rem 0 6rem;
    background: #ffffff;
  }

  .guarantee-card-b {
    max-width: 600px;
    margin: 0 auto;
    padding: 3rem;
    background: linear-gradient(135deg, rgba(67, 233, 123, 0.08), rgba(56, 249, 215, 0.08));
    border-radius: 24px;
    text-align: center;
  }

  .guarantee-icon-b {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .guarantee-title-b {
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 1.5rem;
    font-weight: 700;
    color: #1d1d1f;
    margin-bottom: 0.75rem;
  }

  .guarantee-text-b {
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 1rem;
    color: #86868b;
    margin-bottom: 1.5rem;
  }

  .guarantee-link-b {
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 0.9375rem;
    font-weight: 600;
    color: #667eea;
  }

  .guarantee-link-b:hover {
    color: #764ba2;
  }

  /* Most Opted Indicator */
  .most-opted-indicator {
    display: none;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.75rem 1.25rem;
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.1), rgba(118, 75, 162, 0.1));
    border-radius: 12px;
    margin-bottom: 1rem;
    grid-column: 1 / -1;
  }

  .opted-icon {
    font-size: 1.25rem;
  }

  .opted-text {
    font-family: "Inter", -apple-system, system-ui, sans-serif;
    font-size: 0.875rem;
    font-weight: 600;
    color: #667eea;
  }

  /* Mobile Compact Styles for Variant B Pricing */
  @media (max-width: 767px) {
    .most-opted-indicator {
      display: flex;
    }

    .pricing-card-b {
      padding: 1.5rem;
    }

    .pricing-card-b.pro {
      order: 1;
    }

    .pricing-card-b.free-card {
      order: 2;
    }

    .plan-price-b {
      margin-bottom: 0.25rem;
    }

    .price-amount-b {
      font-size: 2.25rem;
    }

    .features-list-b {
      margin-bottom: 1.5rem;
    }

    .features-list-b li {
      padding: 0.5rem 0;
      font-size: 0.875rem;
    }

    .cta-btn-b {
      padding: 0.875rem 1.5rem;
      font-size: 0.9375rem;
    }

    .yearly-offer-b {
      flex-direction: column;
      gap: 0.5rem;
      padding: 1rem;
      margin-top: 2rem;
    }
  }

  /* Desktop: Hide 'most opted' and reorder for side-by-side */
  @media (min-width: 768px) {
    .pricing-card-b.pro {
      order: 2;
    }

    .pricing-card-b.free-card {
      order: 1;
    }
  }
</style>
